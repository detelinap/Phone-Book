export interface Entry {
  id: number;
  firstName: string;
  lastName: string;
  phoneNumber: string;
}

export interface PhonebookEntry {
  id: number;
  firstName: string;
  lastName: string;
  phoneNumber: string;
}
